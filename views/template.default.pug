if baseURL && baseURL.length>0
  script.
    const baseURL = !{JSON.stringify(baseURL)}
if target_model && target_model.length>0
  script.
    const target_model = !{JSON.stringify(target_model)}
link(rel="stylesheet", href=`${baseURL}/library/bootstrap/css/bootstrap.min.css`)
script(src=`${baseURL}/library/jquery/jquery.min.js`)
script(src=`${baseURL}/library/axios/axios.min.js`)
script(src=`${baseURL}/public/default.js`)